Host *
    AddKeysToAgent yes
    IdentitiesOnly yes
    # The following line should be normally be yes, but since 
    # the storage in the container's .ssh directory is ephemeral 
    # it doesn't provide utility.
    StrictHostKeyChecking no

Host 290server
    HostName comp290fa19.cs.unc.edu
    # User <onyen>

